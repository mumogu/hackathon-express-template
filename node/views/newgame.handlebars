<div class="row">
    <div class="col-sm-10 col-sm-offset-1">
        <h3>Neues Spiel erstellen</h3>

        {{#if has_error}}
            <div class="row">
                <div class="alert alert-danger" role="alert">
                    <strong>Oh snap!</strong> Es gab einen Fehler mit deinem Namen. Versuch es einfach nochmal.
                </div>
            </div>
        {{/if}}

        <form method="POST">
            <div class="form-group">
                <label for="name-input">Spielname</label>
                <input type="text" name="name" class="form-control" id="name-input" placeholder="Kickoff Meeting">
            </div>
            <div class="form-group">
                <label for="words-input">Wörter (min. 16)</label>
                <textarea name="words" class="form-control" if="words-input" rows="20" placeholder="Bullshit goes here"></textarea>
            </div>
            <button type="submit" class="btn btn-default">Submit</button>
        </form>

    </div>
</div>
<br>
<hr>