<div class="row">
    <div class="col-sm-10 col-sm-offset-1">
        <h3>Hier sind alle laufenden Spiele</h3>

        {{#unless games}}
            <p>
                Es sind noch keine Spiele eingetragen. Du kannst aber einfach <a href="/game/create">ein eigenes spiel erstellen.</a>
            </p>
        {{/unless}}

        {{#if games}}
            <p>
                Such dir einfach dein Lieblingsthema aus und klinke dich mit einem Klick in das Spiel ein!
            </p>
        {{/if}}

        <br>
    </div>
</div>



<div class="row">
    {{#each games}}

        <div class="col-sm-4 col-sm-offset-1">

            <div class="panel panel-default">
                <div class="panel-heading">
                    <h3 class="panel-title">{{ this.name }}</h3>
                </div>
                <div class="panel-body">
                    {{ this.words.length }} Wörter, {{ this.players.length }} Spieler
                    <br>
                    <a href="/game/{{ this.name }}" class="btn btn-default" role="button">Mitspielen</a>
                </div>
            </div>

        </div>

    {{/each}}
</div>